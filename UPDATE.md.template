---
title: DJI Link v0.5.0 — pre-release with all functions availible in DJI Link beta
version: 0.5.0
prerelease: true
---

## Highlights

- First release of the C++ rewrite (migrated from the Python beta).

## Added

- All funcs from beta:
    - Camera:
        - rec, photo, mode
        - some small changes to Playback mode
    - Flying
    - RTH
    - Land and takeoff.

## Changed

0 changes.

## Fixed

0 fixes.

<!--
How to cut a release:
  1. Update the fields above (title / version / prerelease) and the text below.
  2. Commit UPDATE.md.
  3. Tag and push:
        git tag v1.0.0
        git push origin v1.0.0
  4. The "Release" workflow builds every platform, computes SHA256 checksums, and
     publishes a GitHub Release using this description.

Everything BELOW the second "---" (except this comment) becomes the release body verbatim.
The "version" field MUST match the git tag (tag v1.0.0 => version: 1.0.0), otherwise the
release fails on purpose — that guards against a version / changelog mismatch.
For a release candidate: version: 1.1.0-rc1 + prerelease: true + tag v1.1.0-rc1.
-->
